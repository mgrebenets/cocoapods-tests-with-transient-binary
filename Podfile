platform :ios, "8.0"

use_frameworks!
inhibit_all_warnings!

target "Breadcrumbs" do
  pod "SplunkMint", path: "splunkmint"
end

target "BreadcrumbsTests" do
#    Uncomment next line, run 'bundle exec pod update --no-repo-update' and the error will be "fixed"
#  pod "SplunkMint", path: "splunkmint"
end
